# 보안망 오프라인 운영안

## 운영 원칙

AutoWork Agent는 보안망의 접근통제나 방화벽을 우회하지 않습니다. 자동 업데이트, 원격 로깅, 클라우드 AI, 외부 웹훅을 사용하지 않으며 로컬 LLM 서버와의 통신만 허용합니다. `dry-run`을 기본으로 사용하고, 실제 입력은 사용자가 계획을 확인한 뒤 승인해야 합니다.

AI는 좌표를 직접 생성하지 않고 프로그램이 수집한 실제 Windows UI Automation 요소의 `element_id`를 선택합니다. 실행 직전 해당 요소의 존재·가시성·활성 상태·bounding box를 다시 검증합니다. 입력·단축키·쓰기·제출·삭제 가능 동작은 자동 재시도하지 않고 사용자 개입을 요구합니다.

## 허용 통신

| 대상 | 허용 주소 예시 | 용도 |
|---|---|---|
| Ollama | `http://127.0.0.1:11434/v1` | 로컬 채팅·비전 모델 호출 |
| LM Studio | `http://127.0.0.1:1234/v1` | 로컬 채팅·비전 모델 호출 |
| 로컬호스트 별칭 | `http://localhost:<포트>/v1` 또는 `http://[::1]:<포트>/v1` | 운영환경별 대체 주소 |

주소는 루프백 호스트의 `/v1` 경로만 허용합니다. 외부 도메인, 다른 컴퓨터, 인증정보, query string, fragment는 차단되며 LLM 요청은 로컬 주소의 `/v1/chat/completions` 경로로만 전송됩니다. 설정 탭의 **연결 점검**은 사용자 프롬프트를 보내지 않고 `/models` 응답만 확인합니다.

## 작업 파일 및 자동화 정책

작업 JSON은 키 입력과 마우스 좌표를 포함할 수 있으므로 신뢰할 수 있는 파일만 불러와야 합니다. 로드·저장 시 버전, 필드, 파일 크기, 단계 수, 동작 종류, 좌표·지연시간·키 이름을 검증합니다. 새로 저장하는 작업에는 이 PC에만 보관되는 HMAC 서명이 붙고, 서명 키는 `%LOCALAPPDATA%\\AutoWorkAgent\\workflow.key`에 저장됩니다. 서명된 파일이 변조되면 불러오기가 거부됩니다. 기존 작업을 덮어쓰기 전에는 `workflow_backups`에 자동 백업되며 최대 20개를 보존합니다. 실행 전에도 현재 화면 크기와 작업 단계를 다시 검증하며 재생은 한 번에 하나만 실행됩니다. 단계별 승인 모드와 실패 단계 재개를 사용할 수 있지만, 재개 전에는 입력 동작이 다시 실행된다는 사실을 사용자에게 확인합니다. 재시작 체크포인트에는 작업 파일 경로·서명·다음 단계·오류 유형만 저장되며 키 입력·문서 내용·전체 단계 목록은 저장하지 않습니다. 복구 시 작업 파일 서명과 단계 범위를 다시 확인합니다.

실행 요약 리포트는 `run_id`, 이벤트, 모드, 작업명, 단계 수·범위, 소요시간, 오류 유형, 정책 프로필만 저장합니다. 설정 파일은 최대 256KB로 제한하며 앱 시작 시 타입·범위·로컬 endpoint·승인 문서 루트 형식을 다시 검증합니다. 손상되거나 과대한 파일, 외부 주소가 포함된 항목은 안전 기본값으로 복원하고 로컬 경고 로그에 기록합니다. 설정 복원은 작업 JSON의 HMAC 서명을 대체하지 않으며, 작업 파일 무결성 검증은 별도로 수행합니다.

자연어 템플릿은 저장 시 정규화된 업무 목표의 fingerprint와 revision을 기록하고, 기존 버전은 템플릿 파일 옆의 숨김 버전 폴더에 최대 20개 보관합니다. fingerprint는 파일 변경을 탐지하기 위한 검증값이며 HMAC 서명을 대신하지 않습니다. 버전 비교 결과는 본문 대신 변경 필드와 메타데이터만 표시합니다.
키 입력·마우스 좌표·입력 문자열·AI 계획 원문·화면 캡처는 실행 리포트에 저장하지 않으며, 리포트 파일은 같은 디렉터리의 고유 임시 파일에 flush·fsync한 뒤 원자적으로 교체되고 최근 100개만 유지됩니다. 작업 JSON·설정·체크포인트·감사 이력·모니터 상태·문서 복구도 동일한 저장 경계를 사용합니다. 실행 리포트 대시보드는 허용된 요약 필드만 읽어 성공률·실패 유형·실패 작업을 로컬에서 계산하고, 템플릿 본문이나 입력 원문을 표시하지 않습니다. 대시보드는 정책 프로필별 통계와 날짜별 추이도 요약하지만 원문을 복원하지 않습니다. 리포트 요약 export는 사용자가 기간과 저장 위치를 직접 지정한 경우에만 수행되며 외부 전송 기능은 없습니다.

AI가 반환할 수 있는 동작은 `click`, `double_click`, `type`, `hotkey`, `scroll`, `wait`뿐입니다. Excel·PDF·HWP·브라우저 전용 어댑터는 현재 화면의 대상 단서만 읽기 전용으로 정규화하며 파일 수정·제출·게시를 수행하지 않습니다. 별도 문서 변경 API는 사용자가 `confirmed=True`로 명시적으로 확인한 경우에만 승인된 단일 로컬 루트의 UTF-8 텍스트 파일 또는 DOCX·XLSX OOXML 패키지를 대상으로 동작하며, 경로 탈출·symlink·지원하지 않는 확장자·중복 확인 문자열·비정상 ZIP 패키지를 차단하고 변경 전 백업과 원자 저장을 수행합니다. 텍스트 파일은 2MiB, Office 패키지와 압축 해제 총량은 각각 20MiB 이하로 제한합니다. DOCX·XLSX 변경 후에는 ZIP 무결성과 대상 XML 재오픈을 `defusedxml` 안전 파서로 확인합니다. `verify_document_change`로 변경 문서와 백업의 SHA-256을 모두 확인한 뒤에만 `restore_document_backup`을 사용하며, 복구에도 명시적 확인이 필요합니다.
직접 좌표, 허용되지 않은 키, 시스템 제어 단축키, 셸 명령, 파일 삭제, 결제, 전송, 게시, 로그인 정보 입력, 결재는 자동 실행 대상이 아닙니다. 비밀번호·주민등록번호·토큰 등 민감정보 필드로 보이는 요소에 대한 자동 입력도 차단합니다. 업무별 안전 프로필은 허용 동작과 승인 수준을 더 좁힐 수 있으며, 복구 계획은 사용자 최종 승인 전까지 절대 실행하지 않습니다. 계획 실행 전에 단계 수·동작/위험도별 개수·총 대기시간·결과 확인 문구 수를 결정적으로 검토하고, 정책 위반 사유가 있으면 실행 버튼과 최종 확인 단계에서 모두 차단합니다.

## 민감정보 보존 및 정리

화면 캡처와 OCR 결과에는 업무 문서나 개인정보가 포함될 수 있습니다. `%LOCALAPPDATA%\AutoWorkAgent\cache`, `errors`, `debug_snapshots`, `workflow_backups`, `run_reports`, `templates` 및 템플릿별 숨김 버전 폴더의 접근권한과 보존기간을 조직 기준으로 관리하십시오.
실행 리포트도 작업명·오류 유형·단계 범위를 노출할 수 있으므로 승인된 운영 담당자만 읽도록 ACL을 제한하십시오. 문서 검색 루트는 담당자가 승인한 최대 5개 디렉터리로 제한하고, 심볼릭 링크·대용량 파일·지원하지 않는 확장자는 건너뜁니다. 검색은 로컬 읽기 전용이며 원본 파일을 수정하거나 외부로 전송하지 않습니다.
프로그램은 시작 시 기본 30일이 지난 캡처·오류 아티팩트와 초과 백업을 정리하지만, 작업 JSON은 자동 삭제하지 않습니다.

진단 컨텍스트와 dry-run 로그의 `text`, `value`, `password`, `token`, `secret`, `authorization`, `api_key` 필드는 마스킹됩니다. 실행 리포트에는 원문 입력과 전체 단계 목록을 기록하지 않지만 작업명과 오류 유형 자체가 업무 정보를 나타낼 수 있으므로 필요 이상으로 외부 공유하지 마십시오. OCR 문자열에서는 이메일·전화번호·주민등록번호 형식을 가능한 범위에서 마스킹하고, 오류 화면 캡처도 OCR로 식별되는 해당 토큰 영역을 가립니다. 화면 캡처와 작업 파일 원문까지 완전히 익명화하는 기능은 아니므로, 업무 종료 후 승인된 절차에 따라 삭제하십시오.

운영 모니터는 30초 주기의 로컬 heartbeat와 실행 통계만 기록합니다. 3건 이상 반복 실패 또는 기준 이하 성공률은 로컬 경보로 표시되며 외부 웹훅·원격 로깅·메일 전송은 사용하지 않습니다. 감사 해시 체인이 변조되거나 단절되면 수동 재생·AI 계획 실행·예약 실행을 모두 중지하고 원인 확인 전 재실행하지 않습니다. 감사 이벤트는 이전 이벤트 해시를 포함하는 SHA-256 체인으로 기록되고, 해시 불일치나 연결 단절이 감지되면 무결성 경보를 표시합니다. 기존 무서명 레거시 기록은 별도 건수로 표시하며 서명된 새 기록과 동일한 신뢰 수준으로 취급하지 않습니다. **지원 패키지 저장**은 상태 스냅샷·최근 감사 이벤트·최근 로그·오류 보고서 목록만 포함하고 Workflow 단계, 키 입력, 화면 캡처 원문은 제외합니다. 패키지를 외부 지원 조직에 전달하기 전에도 조직의 반출 승인과 추가 개인정보 검토를 수행하십시오.

## 오프라인 설치·배포

승인된 준비 환경에서 `requirements.lock.txt`와 모든 전이 의존성 wheel을 확보하고 출처와 해시값을 확인한 뒤 반입합니다. 온라인 환경에서는 다음 명령을 사용합니다.

```powershell
.\install_windows.bat
```

승인된 오프라인 wheel 디렉터리가 `D:\approved-wheels`인 경우에는 다음과 같이 설치합니다.

```powershell
.\install_windows.bat D:\approved-wheels
```

설치가 완료되면 `.venv\READY.txt`가 생성됩니다. `run_windows.bat`과 `build_windows.bat`은 패키지를 자동 다운로드하지 않으며, 설치 완료 환경에서만 실행·빌드합니다. 실행 파일은 다음 명령으로 생성합니다.

```powershell
.\build_windows.bat
```

Python, Tesseract, Ollama 또는 LM Studio의 설치파일과 모델 파일은 조직의 승인된 반입 절차를 따르십시오. Tesseract를 사용할 경우 Windows 실행 파일과 `kor`·`eng` 언어 데이터를 별도로 점검해야 합니다. 저장소 루트에서 `python -m app.release .`를 실행하면 필수 파일, 모든 lock 의존성의 `==` 고정 여부, 설치·실행·빌드 배치 파일의 오프라인 안전 표식을 정적으로 확인할 수 있습니다. 이 검사는 Windows 설치·실행·빌드를 대신하지 않으므로 운영 전 실제 Windows smoke test가 필요합니다. `.github/workflows/windows-smoke.yml`은 GitHub-hosted Windows runner에서 고정 의존성 설치, 런타임 import, 회귀 테스트, PyInstaller 산출물 생성을 자동 확인하지만, 업무용 대상 앱을 열어 입력하거나 문서를 변경하지 않습니다. `tests/test_scenarios.py`도 통제된 local fake를 사용해 저장·재생·복구·AI 안전 게이트의 순서만 검증하며 실제 OS 입력을 보내지 않습니다.

## 운영 전 보안 검토 항목

1. 보안 담당자가 승인한 PC에만 설치합니다.
2. 재시작 복구를 사용하기 전에 체크포인트의 작업 파일 경로와 서명 상태를 확인합니다.
3. 설치파일, wheel, Tesseract 언어 데이터, 모델 파일의 출처와 해시값을 확인합니다.
4. 모델 파일은 외부 서버에 업로드하지 않고 승인된 로컬 저장소에 보관합니다.
5. 방화벽 우회 규칙이나 외부 인바운드 규칙을 추가하지 않습니다.
6. 예약 실행은 앱이 실행 중인 동안에만 사용하며, 앱이 꺼져 있을 때 Windows 작업 스케줄러나 서비스로 자동 시작하도록 설정하지 않습니다. 최근 5회 중 3회 연속 실패하면 회로차단이 예약 실행을 자동 중지하므로, 오류 리포트와 현재 화면을 확인한 뒤 재시작 확인 대화상자에서 1회 재시도를 명시적으로 승인합니다. 원격 제어와 외부 로그 전송 기능은 사용하지 않습니다.
7. 작업 JSON과 로그·캡처·모니터 상태·지원 패키지 폴더의 ACL, 보존기간, 삭제 절차를 조직 기준으로 관리합니다.
8. 감사 무결성 경보가 발생하면 해당 이력을 증거 보존하고 원인 확인 전 수동·AI·예약 자동화 재실행을 중지합니다. 예약 경로는 스케줄러와 회로차단 재시도 승인 상태도 즉시 초기화합니다.
9. 기록·재생 작업은 실행 전에 GUI의 **안전 점검**을 실행하고, 단계 수·동작 종류·기록 화면 크기·대상 창 경고를 확인합니다.
10. 실제 운영 전에는 `dry-run`으로 계획과 UI 요소 매칭을 검증합니다.
11. 예약 실행 전에는 작업의 안전 점검을 수행하고, 기록된 대상 창·화면 크기·단계 수를 확인합니다. 반복 실패 회로차단이 열린 경우 오류 리포트·화면 상태·감사 무결성을 확인하기 전 예약을 재시작하지 않습니다.
12. 실행 리포트 폴더와 대시보드·export가 읽는 진단 데이터의 ACL·보존기간·삭제 절차를 별도로 승인하고, 리포트를 외부 로그나 클라우드로 전송하지 않습니다.
13. 결재, 메일 발송, 문서 게시, 파일 삭제, 개인정보 입력은 사용자가 직접 처리합니다.
14. 배포 전 `python -m app.release .`의 정적 검사 결과와 GitHub Windows Smoke workflow 결과를 보관하고, 별도 승인된 Windows 환경에서 설치·실행·빌드 smoke test를 수행합니다.
15. 시나리오 테스트가 실제 업무용 대상 앱을 열거나 입력하지 않는지 확인하고, 실제 UI 조작 검증은 별도 승인된 테스트 계정·데이터로 수행합니다.
16. 문서 변경 시나리오는 `tmp_path` 격리 폴더의 fixture만 변경하며, 사용자 문서·운영 데이터·공유 폴더를 테스트 대상으로 지정하지 않습니다.

## 배포 검증 권장 명령

```powershell
# 외부 통신 주소와 명령 실행 API 점검
Select-String -Path .\app\*.py -Pattern "https://|subprocess|os.system|shell" -SimpleMatch

# 고정 패키지 목록 확인
Get-Content .\requirements.lock.txt

# 테스트 실행
$env:PYTHONPATH = "."
pytest -q

# 소스 해시 생성
Get-FileHash .\app\engine.py -Algorithm SHA256
Get-FileHash .\app\main.py -Algorithm SHA256
```

`http://127.0.0.1`, `http://localhost`, `http://[::1]`는 로컬 LLM 연결을 위한 기본값입니다. 외부 주소가 추가로 발견되거나 승인되지 않은 기능이 포함되어 있으면 배포하지 말고 소스 검토 후 승인 절차를 다시 진행하십시오.
