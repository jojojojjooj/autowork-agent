# 보안망 오프라인 운영안

## 운영 원칙

AutoWork Agent는 보안망의 접근통제나 방화벽을 우회하지 않습니다. 프로그램은 자동 업데이트, 원격 로깅, 클라우드 AI, 외부 웹훅을 사용하지 않으며 로컬 LLM 서버와의 통신만 허용하는 형태로 운영합니다.

Gemma4 e2b는 화면 좌표를 직접 생성하지 않고, 프로그램이 수집한 실제 UI Automation 요소의 `element_id`를 선택하도록 제한합니다. 프로그램은 실행 직전에 현재 화면에서 해당 요소의 존재·가시성·활성 상태·bounding box를 다시 검증합니다. 이 검증을 통과하지 못하면 실행하지 않고 오류 보고서를 남깁니다. 최초 운영은 dry-run으로 계획만 검증하는 것을 원칙으로 합니다.

## 허용 통신

| 대상 | 허용 주소 예시 | 용도 |
|---|---|---|
| Ollama | `http://127.0.0.1:11434/v1` | 로컬 채팅·비전 모델 호출 |
| LM Studio | `http://127.0.0.1:1234/v1` | 로컬 채팅·비전 모델 호출 |
| 로컬호스트 별칭 | `http://localhost:<포트>/v1` 또는 `http://[::1]:<포트>/v1` | 운영환경에 따른 대체 주소 |

애플리케이션 내부에서 외부 도메인이나 사설망의 다른 호스트 주소를 설정하려고 하면 오류가 발생하도록 되어 있습니다. LLM 요청은 로컬 주소의 `/v1/chat/completions` 경로로만 전송됩니다.

## 자동화 실패 처리

일시적인 UI 요소 미발견, 로딩, 실행 후 기대 문구 미출현은 단계별 화면 재관찰과 제한된 지수 백오프로 최대 3회까지 재시도할 수 있습니다. 팝업, 오류, 로그인 만료, 권한 변경, 제출·전송·삭제·결재와 같은 위험 상태는 자동 재시도하지 않고 즉시 사용자 개입을 요구합니다. 예외가 발생하면 실패 단계와 traceback을 포함한 로컬 오류 보고서를 생성하며, 설정에 따라 당시 화면 캡처도 함께 보관합니다.

## 운영 전 보안 검토 항목

1. 보안 담당자가 승인한 PC에만 프로그램을 설치합니다.
2. Python, Tesseract, Ollama 또는 LM Studio의 설치파일과 모델 파일은 조직의 승인된 반입 절차로 전달하고 해시값을 확인합니다.
3. 모델 파일은 외부 서버에 업로드하지 않고 승인된 로컬 저장소에 보관합니다.
4. Windows 방화벽 정책은 조직의 기준을 따르며, 우회 규칙을 추가하지 않습니다. 일반적으로 로컬 프로세스 간 통신만 필요하므로 외부 인바운드 허용 규칙은 필요하지 않습니다.
5. 자동 실행, 예약 실행, 원격 제어, 외부 로그 전송 기능은 사용하지 않습니다.
6. 업무 파일과 화면 캡처가 포함될 수 있는 `%LOCALAPPDATA%\AutoWorkAgent\cache` 폴더의 보존 기간과 삭제 절차를 조직 기준으로 정합니다.
7. 결재, 메일 발송, 문서 게시, 파일 삭제, 개인정보 입력과 같은 고위험 작업은 자동 실행 대상에서 제외하고 사용자가 직접 처리하도록 합니다.
8. 작업 JSON 파일에는 마우스 좌표와 키 입력이 포함될 수 있으므로 접근권한을 제한하고 암호화된 승인 저장소를 사용하는 것을 권장합니다.

## 로컬 LLM 실행 예시

인터넷이 차단된 환경에서는 설치파일과 모델을 사전에 승인된 반입 절차로 준비해야 합니다. 프로그램 실행 후 **설정** 탭에서 다음 중 하나를 선택합니다.

```text
Ollama:    http://127.0.0.1:11434/v1
LM Studio: http://127.0.0.1:1234/v1
```

모델이 실행 중인지 확인하는 과정은 해당 제품의 로컬 관리 화면에서 수행합니다. AutoWork Agent 자체는 외부 네트워크를 사용하여 모델을 내려받지 않습니다.

## 배포 검증 권장 명령

보안 담당자는 승인된 검사 환경에서 다음 항목을 확인할 수 있습니다.

```powershell
# 외부 통신이 필요한 문자열이 소스에 들어 있는지 점검하는 예시
Select-String -Path .\app\*.py -Pattern "https://|http://" -SimpleMatch

# 패키지 목록 확인
Get-Content .\requirements.txt

# 소스 해시 생성
Get-FileHash .\app\engine.py -Algorithm SHA256
Get-FileHash .\app\main.py -Algorithm SHA256
```

`http://127.0.0.1`과 `http://localhost`는 로컬 LLM 연결을 위한 기본값입니다. 외부 주소가 추가로 발견되거나, 보안 담당자가 허용하지 않은 기능이 포함되어 있으면 배포하지 말고 소스 검토 후 승인 절차를 다시 진행해야 합니다.
